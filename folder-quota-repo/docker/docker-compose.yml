version: '3.4'
services:
#  Optional
#  folder-quota-repo-share:
#    image: ${docker.share.image}:${alfresco.share.version}
#    environment:
#      REPO_HOST: folder-quota-repo-acs
#      REPO_PORT: 8080
#    ports:
#      - "${share.port}:8080"
  folder-quota-repo-acs:
    image: alfresco-content-services-folder-quota-repo:development
    build:
      dockerfile: ./Dockerfile
      context: ../../../target
    environment:
      CATALINA_OPTS: "-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=0.0.0.0:8888"
    ports:
      - "${acs.port}:8080"
      - "${acs.debug.port}:8888"
    volumes:
      - folder-quota-repo-acs-volume:/usr/local/tomcat/alf_data
    depends_on:
      - folder-quota-repo-postgres
  folder-quota-repo-postgres:
    image: postgres:9.6
    environment:
      POSTGRES_DB: alfresco
      POSTGRES_USER: alfresco
      POSTGRES_PASSWORD: alfresco
    command: postgres -c max_connections=300 -c log_min_messages=LOG
    ports:
      - "${postgres.port}:5432"
    volumes:
      - folder-quota-repo-db-volume:/var/lib/postgresql/data
  folder-quota-repo-ass:
    image: alfresco/alfresco-search-services:1.2.0
    environment:
      SOLR_ALFRESCO_HOST: folder-quota-repo-acs
      SOLR_ALFRESCO_PORT: 8080
      SOLR_SOLR_HOST: folder-quota-repo-ass
      SOLR_SOLR_PORT: 8983
      SOLR_CREATE_ALFRESCO_DEFAULTS: alfresco,archive
    ports:
      - "8983:8983"
    volumes:
      - folder-quota-repo-ass-volume:/opt/alfresco-search-services/contentstore
      - folder-quota-repo-ass-volume:/opt/alfresco-search-services/data
volumes:
  folder-quota-repo-acs-volume:
    external: true
  folder-quota-repo-db-volume:
    external: true
  folder-quota-repo-ass-volume:
    external: true